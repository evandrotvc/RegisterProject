{
    "type": "postgres",
    "host": "localhost",
    "port": 5432,
    "username": "postgres",
    "password": "postgres",
    "database": "registerProject",
    "synchronize": true,
    "url": "postgres://postgres:postgres@your_IPMachine/registerProject",
    "logging": false,
    "entities" : [
        "./src/models/*.ts"
    ],
    "migrations": [
        "./src/database/migrations/*.ts"
    ],
    "cli": {
        "migrationsDir" : "./src/database/migrations"
    }
}
